beautifulsoup4==4.12.3
requests==2.31.0
python-dotenv==1.0.0

# LLM providers (install at least one)
openai>=1.0.0
google-generativeai>=0.3.0

# Optional: Install tavily-python for better IR page discovery
# Note: May require Rust compiler. Scraper will use fallback if not available.
# tavily-python==0.3.0
